package com.textify.app.domain.usecase

class TranscribeAudioUseCase {
    operator fun invoke(audioPath: String): String {
        // Lógica para transcribir audio a texto
        return "Texto transcripto"
    }
}
